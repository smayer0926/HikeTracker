{{#partial "content"}}
    {{#if newLocation}}
        <p>Location</p>
        <ul>
            <li>State:<br> {{newLocation.locationState}}</li><br><br>
        </ul>
        <a href="/"><button type="button" class="btn btn-default">Back to Main Page</button></a>
    {{else}}
        <p>No locations</p>
    {{/if}}

{{/partial}}

{{> layout.hbs}}